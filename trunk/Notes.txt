To do:
    -Tray
    -Icon
    -Win Installer that installs dependencies automatically
    -Web site with user comments
    -Load GUI options from config file
    -Harmonize comments with Doxygen
    -Check if it is worth using the UI builder
    -Scope colors
    -Memory leaks
    -CPU consuption (scope)
    -Have the down/up speed averaged over 'N' samples
    -Scope: Have the different data match strings
    -Save daily stats in a file
    -Console needs to be able to get message from GUI too
    -Show/Hide parts of the widget (console and device select)
    -Seperate the GUI build commands in functions (getting too big)

Bugs:
